import { StyleSheet } from 'react-native'

export const typography = StyleSheet.create({
    subtitle: {
        fontWeight: '500',
        fontSize: 16,
        lineHeight: 25,
        letterSpacing: 0.15,
    },
    label: {
        fontWeight: '500',
        fontSize: 12,
        lineHeight: 16,
        letterSpacing: 0.5,
    }
})
